'use strict';

describe('Service: scheduleReportService', function () {

  // load the service's module
  beforeEach(module('ts5App'));

  // instantiate service
  var scheduleReportService;
  beforeEach(inject(function (_scheduleReportService_) {
    scheduleReportService = _scheduleReportService_;
  }));

  it('should do something', function () {
    //expect(!!scheduleReportService).toBe(true);
  });

});
