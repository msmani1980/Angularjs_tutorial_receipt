'use strict';

describe('Service: eposTransactionFactory', function () {

  // load the service's module
  beforeEach(module('ts5App'));

  // instantiate service
  var eposTransactionFactory;
  beforeEach(inject(function (_eposTransactionFactory_) {
    eposTransactionFactory = _eposTransactionFactory_;
  }));

});
